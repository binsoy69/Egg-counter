---
phase: 04-remote-access-and-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - deploy/egg-counter-dashboard.service
  - deploy/egg-counter-detector.service
  - deploy/cloudflared-eggsentry.service
  - deploy/cloudflared-config.yml
  - deploy/egg-counter.env.example
  - docs/remote-access.md
  - tests/test_pipeline.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "Dashboard, detector, and tunnel each have a concrete supervised runtime definition for Raspberry Pi boot and crash recovery"
    - "Deployment artifacts document the exact environment contract for auth secrets, model path, config paths, and tunnel origin"
    - "The detector and dashboard processes are restartable independently, and the detector service is configured to keep retrying after crashes"
    - "Remote access setup is documented around Cloudflare Tunnel and the existing FastAPI dashboard origin"
  artifacts:
    - path: "deploy/egg-counter-dashboard.service"
      provides: "systemd unit for the FastAPI dashboard runtime"
      contains: "Restart=always"
    - path: "deploy/egg-counter-detector.service"
      provides: "systemd unit for the detection runtime"
      contains: "egg-counter run"
    - path: "deploy/cloudflared-config.yml"
      provides: "Cloudflare Tunnel example configuration for routing HTTPS traffic to the local dashboard"
      contains: "ingress:"
    - path: "deploy/egg-counter.env.example"
      provides: "Environment contract for auth secrets and runtime paths"
      contains: "EGG_COUNTER_AUTH_USERNAME="
    - path: "docs/remote-access.md"
      provides: "Operator instructions for install, enable, validate, reboot test, and crash-restart test"
      contains: "systemctl enable"
  key_links:
    - from: "deploy/egg-counter-dashboard.service"
      to: "src/egg_counter/cli.py"
      via: "service runs the exact `egg-counter serve` command against the repo config and zone files"
      pattern: "egg-counter serve"
    - from: "deploy/egg-counter-detector.service"
      to: "src/egg_counter/pipeline.py"
      via: "service supervises the live detector runtime independently of the dashboard"
      pattern: "egg-counter run"
    - from: "deploy/cloudflared-config.yml"
      to: "deploy/egg-counter-dashboard.service"
      via: "tunnel origin points to the dashboard service on localhost:8000"
      pattern: "service: http://127.0.0.1:8000"
---

<objective>
Add the deployment and operations artifacts that make the authenticated dashboard remotely reachable through Cloudflare Tunnel and reliable across Pi reboot/crash scenarios.

Purpose: turn the existing app plus new auth layer into a production-ready Raspberry Pi service layout.
Output: systemd units, environment example, Cloudflare Tunnel config/example, operator documentation, and regression coverage for manual collection/restart-sensitive runtime behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-remote-access-and-hardening/04-CONTEXT.md
@.planning/phases/04-remote-access-and-hardening/04-RESEARCH.md
@.planning/phases/04-remote-access-and-hardening/04-VALIDATION.md
@.planning/phases/04-remote-access-and-hardening/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add concrete systemd unit files and environment contract for Pi deployment</name>
  <files>deploy/egg-counter-dashboard.service, deploy/egg-counter-detector.service, deploy/egg-counter.env.example, tests/test_pipeline.py</files>
  <read_first>
    - src/egg_counter/cli.py
    - src/egg_counter/pipeline.py
    - config/settings.yaml
    - .planning/phases/04-remote-access-and-hardening/04-RESEARCH.md
    - .planning/phases/04-remote-access-and-hardening/04-01-PLAN.md
  </read_first>
  <action>
    Create `deploy/egg-counter-dashboard.service` with exact directives:
    - `Description=Egg Counter dashboard service`
    - `After=network-online.target`
    - `Wants=network-online.target`
    - `Type=simple`
    - `WorkingDirectory=/opt/egg-counter`
    - `EnvironmentFile=/etc/egg-counter/egg-counter.env`
    - `ExecStart=/usr/bin/env egg-counter serve --config /opt/egg-counter/config/settings.yaml --zone /opt/egg-counter/config/zone.json --host 127.0.0.1 --port 8000`
    - `Restart=always`
    - `RestartSec=5`
    - `WantedBy=multi-user.target`

    Create `deploy/egg-counter-detector.service` with the same service shell but exact detector command:
    - `ExecStart=/usr/bin/env egg-counter run --model ${EGG_COUNTER_MODEL_PATH} --config /opt/egg-counter/config/settings.yaml --zone /opt/egg-counter/config/zone.json`
    - `Restart=always`
    - `RestartSec=5`

    Create `deploy/egg-counter.env.example` with exact keys:
    - `EGG_COUNTER_MODEL_PATH=/opt/egg-counter/models/egg-model`
    - `EGG_COUNTER_AUTH_USERNAME=admin`
    - `EGG_COUNTER_AUTH_PASSWORD_HASH=scrypt$replace-salt$replace-hash`
    - `EGG_COUNTER_SESSION_SECRET=replace-with-long-random-secret`
    - `EGG_COUNTER_SESSION_MAX_AGE=1209600`

    Update `tests/test_pipeline.py` only if needed to keep runtime coverage aligned with the detector command assumptions; do not add fake systemd tests there unless it verifies an existing detector behavior that changed.
  </action>
  <verify>
    <automated>rg -n "Restart=always|EnvironmentFile=|egg-counter serve|egg-counter run|EGG_COUNTER_AUTH_USERNAME=" deploy</automated>
  </verify>
  <acceptance_criteria>
    - deploy/egg-counter-dashboard.service contains `ExecStart=/usr/bin/env egg-counter serve`
    - deploy/egg-counter-dashboard.service contains `EnvironmentFile=/etc/egg-counter/egg-counter.env`
    - deploy/egg-counter-dashboard.service contains `Restart=always`
    - deploy/egg-counter-detector.service contains `ExecStart=/usr/bin/env egg-counter run --model ${EGG_COUNTER_MODEL_PATH}`
    - deploy/egg-counter-detector.service contains `RestartSec=5`
    - deploy/egg-counter.env.example contains `EGG_COUNTER_AUTH_USERNAME=`
    - deploy/egg-counter.env.example contains `EGG_COUNTER_AUTH_PASSWORD_HASH=`
    - deploy/egg-counter.env.example contains `EGG_COUNTER_SESSION_SECRET=`
  </acceptance_criteria>
  <done>Pi deployment has explicit supervised runtime definitions and a documented secret/runtime environment contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add Cloudflare Tunnel example config and operator runbook for remote validation</name>
  <files>deploy/cloudflared-eggsentry.service, deploy/cloudflared-config.yml, docs/remote-access.md</files>
  <read_first>
    - deploy/egg-counter-dashboard.service
    - src/egg_counter/web/server.py
    - .planning/phases/04-remote-access-and-hardening/04-RESEARCH.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
    Create `deploy/cloudflared-config.yml` with exact example content shape:
    - `tunnel: eggsentry`
    - `credentials-file: /etc/cloudflared/eggsentry.json`
    - `ingress:`
    - hostname entry `eggs.example.trycloudflare.com` or clearly marked placeholder hostname
    - `service: http://127.0.0.1:8000`
    - terminal fallback `service: http_status:404`

    Create `deploy/cloudflared-eggsentry.service` with exact directives:
    - `Description=Cloudflare Tunnel for Egg Counter dashboard`
    - `After=network-online.target egg-counter-dashboard.service`
    - `Requires=egg-counter-dashboard.service`
    - `ExecStart=/usr/bin/cloudflared tunnel --config /opt/egg-counter/deploy/cloudflared-config.yml run eggsentry`
    - `Restart=always`
    - `RestartSec=5`
    - `WantedBy=multi-user.target`

    Create `docs/remote-access.md` with exact sections:
    - `## Install and enable services`
    - `## Configure auth secrets`
    - `## Configure Cloudflare Tunnel`
    - `## Validation`
    - `## Reboot test`
    - `## Crash recovery test`

    The runbook must include these exact commands:
    - `sudo systemctl daemon-reload`
    - `sudo systemctl enable egg-counter-dashboard egg-counter-detector cloudflared-eggsentry`
    - `sudo systemctl start egg-counter-dashboard egg-counter-detector cloudflared-eggsentry`
    - `sudo systemctl status egg-counter-dashboard egg-counter-detector cloudflared-eggsentry`
    - `journalctl -u egg-counter-detector -n 100 --no-pager`
  </action>
  <verify>
    <automated>rg -n "cloudflared|ingress:|http://127.0.0.1:8000|systemctl enable|journalctl -u egg-counter-detector" deploy docs</automated>
  </verify>
  <acceptance_criteria>
    - deploy/cloudflared-config.yml contains `service: http://127.0.0.1:8000`
    - deploy/cloudflared-config.yml contains `ingress:`
    - deploy/cloudflared-eggsentry.service contains `Requires=egg-counter-dashboard.service`
    - deploy/cloudflared-eggsentry.service contains `/usr/bin/cloudflared tunnel`
    - docs/remote-access.md contains `## Validation`
    - docs/remote-access.md contains `sudo systemctl enable egg-counter-dashboard egg-counter-detector cloudflared-eggsentry`
    - docs/remote-access.md contains `journalctl -u egg-counter-detector -n 100 --no-pager`
  </acceptance_criteria>
  <done>Remote access deployment is concretely documented around Cloudflare Tunnel and the authenticated local dashboard origin.</done>
</task>

</tasks>

<verification>
1. `rg -n "Restart=always|ExecStart=|EnvironmentFile=|cloudflared|service: http://127.0.0.1:8000" deploy`
2. `rg -n "systemctl enable|systemctl start|systemctl status|journalctl -u egg-counter-detector" docs/remote-access.md`
3. `python -m pytest tests/ -x -q`
</verification>

<success_criteria>
- Detector, dashboard, and tunnel all have concrete `systemd`/deployment definitions
- Deployment environment contract includes auth secrets and model path
- Tunnel config targets the existing local dashboard service over loopback
- Operator runbook covers install, enable, start, validation, reboot, and crash recovery
- Phase 4 execution can verify both INFRA-01 and INFRA-02 without inventing deployment details during implementation
</success_criteria>

<output>
After completion, create `.planning/phases/04-remote-access-and-hardening/04-02-SUMMARY.md`
</output>
