---
phase: 04-remote-access-and-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/egg_counter/config.py
  - config/settings.yaml
  - src/egg_counter/auth.py
  - src/egg_counter/web/server.py
  - src/egg_counter/web/templates/login.html
  - tests/test_web_api.py
  - tests/test_websocket.py
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "Remote dashboard routes are not anonymously accessible; unauthenticated users are redirected to or blocked by application login"
    - "Successful login establishes a signed session that grants access to dashboard HTML, API routes, and websocket connections"
    - "Authentication uses a single configured username and password hash loaded from environment-aware settings rather than Cloudflare Access"
    - "The existing /health route remains available for local and tunnel health verification without requiring a user session"
  artifacts:
    - path: "src/egg_counter/auth.py"
      provides: "Single-user authentication helpers for password verification and session state"
      exports: ["require_authenticated_request", "verify_password", "build_session_middleware_config"]
      contains: "def verify_password("
    - path: "src/egg_counter/web/server.py"
      provides: "Login/logout routes and auth enforcement for HTML, API, and websocket endpoints"
      contains: "@app.post(\"/login\")"
    - path: "src/egg_counter/web/templates/login.html"
      provides: "Phone-usable login screen for the remote dashboard"
      contains: "<form"
    - path: "tests/test_web_api.py"
      provides: "Route-level auth coverage"
      contains: "test_login_sets_session_cookie"
    - path: "tests/test_websocket.py"
      provides: "Websocket auth coverage"
      contains: "test_websocket_requires_authenticated_session"
  key_links:
    - from: "src/egg_counter/config.py"
      to: "src/egg_counter/web/server.py"
      via: "settings loader provides auth_username, auth_password_hash, and session_secret to the FastAPI app"
      pattern: "auth_username|auth_password_hash|session_secret"
    - from: "src/egg_counter/web/server.py"
      to: "src/egg_counter/auth.py"
      via: "login route and route guards delegate password/session checks to auth helpers"
      pattern: "verify_password|require_authenticated"
    - from: "tests/test_web_api.py"
      to: "src/egg_counter/web/server.py"
      via: "login/logout and unauthorized access tests prove route protection"
      pattern: "login|401|redirect"
---

<objective>
Add application-managed authentication to the existing FastAPI dashboard so the remotely exposed dashboard is protected by a single-user login flow.

Purpose: satisfy the remote-access security boundary for Phase 4 without changing the existing monolith or introducing external identity services.
Output: environment-aware auth config, login/logout flow, session enforcement across HTML/API/websocket routes, and regression tests proving remote access is not anonymous.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-remote-access-and-hardening/04-CONTEXT.md
@.planning/phases/04-remote-access-and-hardening/04-RESEARCH.md
@.planning/phases/04-remote-access-and-hardening/04-VALIDATION.md

<interfaces>
From src/egg_counter/config.py:
```python
def load_settings(path: str = "config/settings.yaml") -> dict:
```

From src/egg_counter/web/server.py:
```python
def create_app(settings: dict, zone_config: dict, pipeline: EggCounterPipeline | None = None) -> FastAPI:
```

From current config/settings.yaml:
```yaml
web_host: "0.0.0.0"
web_port: 8000
dashboard_title: "EggSentry"
collection_mode: "manual"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add environment-aware auth settings and a dedicated auth helper module</name>
  <files>src/egg_counter/config.py, config/settings.yaml, src/egg_counter/auth.py, tests/test_web_api.py</files>
  <read_first>
    - src/egg_counter/config.py
    - config/settings.yaml
    - src/egg_counter/web/server.py
    - .planning/phases/04-remote-access-and-hardening/04-CONTEXT.md
    - .planning/phases/04-remote-access-and-hardening/04-RESEARCH.md
  </read_first>
  <action>
    Update `src/egg_counter/config.py` so `load_settings()` overlays exact environment keys onto the YAML settings dict:
    - `EGG_COUNTER_AUTH_USERNAME` -> `auth_username`
    - `EGG_COUNTER_AUTH_PASSWORD_HASH` -> `auth_password_hash`
    - `EGG_COUNTER_SESSION_SECRET` -> `session_secret`
    - `EGG_COUNTER_SESSION_MAX_AGE` -> `session_max_age` as int

    Keep non-secret defaults in `config/settings.yaml` by adding exact keys:
    - `auth_enabled: true`
    - `auth_cookie_name: "egg_counter_session"`
    - `session_max_age: 1209600`
    Do not store plaintext credentials or hashes in `settings.yaml`.

    Create `src/egg_counter/auth.py` with concrete helpers:
    - `def hash_password(password: str, salt: str) -> str:` using `hashlib.scrypt`
    - `def verify_password(password: str, stored_hash: str) -> bool:` where `stored_hash` format is exactly `scrypt$<salt>$<hex>`
    - `def build_session_middleware_config(settings: dict) -> dict:` returning `secret_key`, `session_cookie`, `max_age`, `same_site="lax"`, `https_only=True`
    - `def is_authenticated(request: Request) -> bool:` reading `request.session["authenticated"]`

    In `tests/test_web_api.py`, add exact auth-unit style cases:
    - `test_verify_password_accepts_matching_scrypt_hash`
    - `test_verify_password_rejects_wrong_password`
    - `test_load_settings_overrides_auth_from_environment`
  </action>
  <verify>
    <automated>python -m pytest tests/test_web_api.py -k "verify_password or load_settings_overrides_auth_from_environment" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/egg_counter/config.py contains `EGG_COUNTER_AUTH_USERNAME`
    - src/egg_counter/config.py contains `EGG_COUNTER_AUTH_PASSWORD_HASH`
    - src/egg_counter/config.py contains `EGG_COUNTER_SESSION_SECRET`
    - config/settings.yaml contains `auth_enabled: true`
    - config/settings.yaml contains `auth_cookie_name: "egg_counter_session"`
    - src/egg_counter/auth.py contains `def verify_password(`
    - src/egg_counter/auth.py contains `hashlib.scrypt`
    - src/egg_counter/auth.py contains `scrypt$`
    - tests/test_web_api.py contains `test_verify_password_accepts_matching_scrypt_hash`
    - tests/test_web_api.py contains `test_load_settings_overrides_auth_from_environment`
  </acceptance_criteria>
  <done>Settings loader and auth helper module support secure single-user login configuration without committing secrets into the repo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Protect dashboard, API, and websocket routes with login/logout flow</name>
  <files>src/egg_counter/web/server.py, src/egg_counter/web/templates/login.html, tests/test_web_api.py, tests/test_websocket.py</files>
  <read_first>
    - src/egg_counter/web/server.py
    - src/egg_counter/web/templates/dashboard.html
    - src/egg_counter/web/templates/history.html
    - src/egg_counter/auth.py
    - tests/test_web_api.py
    - tests/test_websocket.py
  </read_first>
  <action>
    Update `src/egg_counter/web/server.py` to add `SessionMiddleware` using the config returned by `build_session_middleware_config(settings)`.

    Add exact routes:
    - `GET /login`
    - `POST /login`
    - `POST /logout`

    Add exact auth behavior:
    - unauthenticated `GET /`, `GET /dashboard`, and `GET /history` redirect to `/login`
    - unauthenticated `GET /api/dashboard`, `GET /api/history`, `POST /api/collect`, and `GET /ws-meta` return `401`
    - unauthenticated websocket connects to `/ws` and `/ws/dashboard` are rejected before the initial snapshot is sent
    - `/health` stays open without auth

    Implement `POST /login` to:
    - read `username` and `password` from form data
    - compare against `settings["auth_username"]` and `verify_password(password, settings["auth_password_hash"])`
    - set `request.session["authenticated"] = True` and `request.session["username"] = <configured username>` on success
    - redirect to `/dashboard` on success
    - return `401` with a visible error on failure

    Implement `POST /logout` to clear the session and redirect to `/login`.

    Create `src/egg_counter/web/templates/login.html` with a mobile-usable form containing exact inputs:
    - `name="username"`
    - `name="password"`
    - submit button text `Log In`

    Add exact tests:
    - `test_dashboard_redirects_to_login_when_unauthenticated`
    - `test_api_dashboard_returns_401_when_unauthenticated`
    - `test_login_sets_session_cookie`
    - `test_logout_clears_session`
    - `test_websocket_requires_authenticated_session`
  </action>
  <verify>
    <automated>python -m pytest tests/test_web_api.py tests/test_websocket.py -k "login or logout or unauthenticated or websocket_requires_authenticated_session" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/egg_counter/web/server.py contains `SessionMiddleware`
    - src/egg_counter/web/server.py contains `@app.get("/login")`
    - src/egg_counter/web/server.py contains `@app.post("/login")`
    - src/egg_counter/web/server.py contains `@app.post("/logout")`
    - src/egg_counter/web/server.py contains `request.session["authenticated"] = True`
    - src/egg_counter/web/server.py contains `return RedirectResponse(url="/login"`
    - src/egg_counter/web/templates/login.html contains `name="username"`
    - src/egg_counter/web/templates/login.html contains `name="password"`
    - tests/test_web_api.py contains `test_login_sets_session_cookie`
    - tests/test_web_api.py contains `test_api_dashboard_returns_401_when_unauthenticated`
    - tests/test_websocket.py contains `test_websocket_requires_authenticated_session`
  </acceptance_criteria>
  <done>Remote-facing app routes are protected by a working single-user session login flow.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_web_api.py tests/test_websocket.py -x -q`
2. `rg -n "@app.get\\(\"/login\"\\)|@app.post\\(\"/login\"\\)|@app.post\\(\"/logout\"\\)" src/egg_counter/web/server.py`
3. `rg -n "auth_enabled|auth_cookie_name|session_max_age" config/settings.yaml src/egg_counter/config.py`
</verification>

<success_criteria>
- FastAPI dashboard is protected by app-managed authentication rather than anonymous remote access
- Authentication configuration is environment-driven and does not store plaintext credentials in repo config
- HTML, API, and websocket routes all enforce the same session boundary
- `/health` remains accessible for operational checks
- Regression tests prove unauthorized access is blocked and login establishes a session
</success_criteria>

<output>
After completion, create `.planning/phases/04-remote-access-and-hardening/04-01-SUMMARY.md`
</output>
