---
phase: 03-web-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - config/settings.yaml
  - src/egg_counter/pipeline.py
  - src/egg_counter/tracker.py
  - src/egg_counter/repository.py
  - src/egg_counter/cli.py
  - src/egg_counter/web/__init__.py
  - src/egg_counter/web/server.py
  - src/egg_counter/web/realtime.py
  - src/egg_counter/web/schemas.py
  - tests/test_web_api.py
  - tests/test_websocket.py
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "The project exposes a FastAPI server on the local network that serves dashboard data from the Phase 2 SQLite database"
    - "A WebSocket endpoint broadcasts new egg and collection events within the same runtime rather than relying on polling"
    - "A dashboard collection action persists a collection event and returns an updated summary snapshot"
  artifacts:
    - path: "src/egg_counter/web/server.py"
      provides: "FastAPI app, HTML routes, JSON API routes, collection action, and WebSocket endpoint"
      exports: ["create_app"]
      contains: "FastAPI("
    - path: "src/egg_counter/web/realtime.py"
      provides: "Connection manager and broadcast helpers for dashboard clients"
      exports: ["WebSocketHub"]
      contains: "class WebSocketHub"
    - path: "src/egg_counter/web/schemas.py"
      provides: "Canonical response and event models shared by tests and routes"
      contains: "class DashboardSnapshot"
    - path: "tests/test_web_api.py"
      provides: "HTTP/API coverage for dashboard, history, and collection actions"
      contains: "def test_dashboard_snapshot"
    - path: "tests/test_websocket.py"
      provides: "WebSocket coverage for connect, broadcast, and disconnect behavior"
      contains: "def test_websocket_receives_egg_detected_event"
  key_links:
    - from: "src/egg_counter/web/server.py"
      to: "src/egg_counter/repository.py"
      via: "dashboard and history routes query through EggRepository methods"
      pattern: "EggRepository"
    - from: "src/egg_counter/pipeline.py"
      to: "src/egg_counter/web/realtime.py"
      via: "persisted pipeline events are forwarded to the WebSocket broadcaster callback"
      pattern: "event_callback"
    - from: "src/egg_counter/web/server.py"
      to: "src/egg_counter/db.py"
      via: "collection endpoint persists via EggDatabaseLogger-compatible collection model"
      pattern: "log_eggs_collected"
---

<objective>
Build the backend surface area for the Phase 3 dashboard: web server, JSON and WebSocket contracts, runtime event bridge, and collection API.

Purpose: Make persisted egg data available to a local-network phone browser and push live updates without polling.
Output: FastAPI app, repository-backed dashboard/history APIs, WebSocket broadcast path for `egg_detected` / `eggs_collected`, collection endpoint, and backend tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-web-dashboard/03-CONTEXT.md
@.planning/phases/03-web-dashboard/03-RESEARCH.md
@.planning/phases/03-web-dashboard/03-VALIDATION.md

<interfaces>
From src/egg_counter/pipeline.py:
```python
class EggCounterPipeline:
    def __init__(self, settings: dict, zone_config: dict) -> None: ...
    def process_frame(self, frame, timestamp: float) -> list[dict]: ...
    def run(self, model_path: str, camera_index: int = 0, video_path: str | None = None) -> None: ...
```

From src/egg_counter/repository.py:
```python
class EggRepository:
    def get_daily_summary(self, target_date: date) -> dict: ...
    def get_eggs_by_date_range(self, start: date, end: date) -> list[dict]: ...
    def get_size_breakdown(self, start: date, end: date) -> dict: ...
```

From src/egg_counter/db.py:
```python
class EggDatabaseLogger:
    def log_egg_detected(...) -> dict: ...
    def log_eggs_collected(self, count: int) -> dict: ...
```

From config/settings.yaml:
```yaml
camera_index: 0
db_path: "data/eggs.db"
frame_rate: 3
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 3 dependencies, server config, and dashboard query primitives</name>
  <files>pyproject.toml, config/settings.yaml, src/egg_counter/repository.py, tests/test_web_api.py</files>
  <read_first>
    - pyproject.toml
    - config/settings.yaml
    - src/egg_counter/repository.py
    - tests/test_repository.py
    - .planning/phases/03-web-dashboard/03-RESEARCH.md
  </read_first>
  <action>
    Update `pyproject.toml` dependencies to include `fastapi>=0.115`, `uvicorn[standard]>=0.32`, and `jinja2>=3.1` in the main dependencies list, and add `httpx>=0.28` plus `pytest-asyncio>=0.24` under `[project.optional-dependencies].dev`.

    Update `config/settings.yaml` to add exact web runtime keys:
    - `web_host: "0.0.0.0"`
    - `web_port: 8000`
    - `web_reload: false`
    - `dashboard_title: "EggSentry"`
    - `collection_mode: "manual"`

    Extend `EggRepository` with concrete methods the web layer can use directly:
    - `get_history_records(self, start: date | None = None, end: date | None = None, size: str | None = None, limit: int = 200) -> list[dict]`
    - `get_best_day(self) -> dict`
    - `get_all_time_totals(self) -> dict`
    - `get_top_size(self) -> dict`
    - `get_dashboard_snapshot(self, target_date: date, period: str) -> dict`

    Implement `get_dashboard_snapshot()` around the post-collection running count, not the raw daily total. It must:
    - find the most recent same-day `collection_events.timestamp`
    - count only `egg_events` with `timestamp > latest_collection_timestamp` when a same-day collection exists
    - otherwise count all of today's egg events

    Implement exact return keys for `get_dashboard_snapshot()`:
    - `date`
    - `today_total` as the current running total after the latest same-day collection
    - `today_by_size` with keys `small`, `medium`, `large`, `jumbo`, each representing only eggs counted after the latest same-day collection
    - `all_time_total`
    - `best_day` as `{date, total}`
    - `top_size` as `{size, total}`
    - `period`
    - `production_series` as list of `{date, total}`
    - `size_breakdown` as `{small, medium, large, jumbo}`

    Create `tests/test_web_api.py` now with repository-backed API-focused fixtures and a seeded test database helper. Include exact tests:
    - `test_dashboard_snapshot`
    - `test_history_records_default_newest_first`
    - `test_history_records_filters_by_size_and_date_range`
    - `test_best_day_and_top_size_roll_up`

    Use the existing SQLite schema from `EggDatabaseLogger`; do not duplicate schema SQL in tests.
    Add one fixture case where a same-day collection exists and assert that pre-collection eggs do not appear in `today_total` or `today_by_size`.
  </action>
  <verify>
    <automated>cd D:/codeNcraft/Egg-counter && python -m pytest tests/test_web_api.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `fastapi>=0.115`
    - pyproject.toml contains `uvicorn[standard]>=0.32`
    - pyproject.toml contains `jinja2>=3.1`
    - pyproject.toml contains `httpx>=0.28`
    - pyproject.toml contains `pytest-asyncio>=0.24`
    - config/settings.yaml contains `web_host: "0.0.0.0"`
    - config/settings.yaml contains `web_port: 8000`
    - config/settings.yaml contains `collection_mode: "manual"`
    - src/egg_counter/repository.py contains `def get_history_records(`
    - src/egg_counter/repository.py contains `def get_best_day(`
    - src/egg_counter/repository.py contains `def get_all_time_totals(`
    - src/egg_counter/repository.py contains `def get_top_size(`
    - src/egg_counter/repository.py contains `def get_dashboard_snapshot(`
    - src/egg_counter/repository.py contains `SELECT MAX(timestamp) FROM collection_events`
    - tests/test_web_api.py contains `test_dashboard_snapshot`
    - tests/test_web_api.py contains `test_dashboard_snapshot_excludes_pre_collection_eggs`
    - tests/test_web_api.py contains `test_history_records_default_newest_first`
    - `python -m pytest tests/test_web_api.py -x -q` exits 0
  </acceptance_criteria>
  <done>Dependencies, config keys, and repository query primitives exist for the web server to consume without ad hoc SQL in route handlers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the FastAPI app, WebSocket hub, and collection/history/dashboard endpoints</name>
  <files>src/egg_counter/web/__init__.py, src/egg_counter/web/realtime.py, src/egg_counter/web/schemas.py, src/egg_counter/web/server.py, tests/test_web_api.py, tests/test_websocket.py</files>
  <read_first>
    - src/egg_counter/repository.py
    - src/egg_counter/db.py
    - src/egg_counter/cli.py
    - .planning/phases/03-web-dashboard/03-CONTEXT.md
    - .planning/phases/03-web-dashboard/03-RESEARCH.md
  </read_first>
  <action>
    Create `src/egg_counter/web/realtime.py` with class `WebSocketHub` and exact methods:
    - `connect(self, websocket: WebSocket) -> None`
    - `disconnect(self, websocket: WebSocket) -> None`
    - `broadcast_json(self, payload: dict) -> None`
    - `build_snapshot_event(self, event_type: str, snapshot: dict, toast: str | None = None) -> dict`

    Create `src/egg_counter/web/schemas.py` with Pydantic models named exactly:
    - `SizeBreakdown`
    - `BestDay`
    - `TopSize`
    - `ProductionPoint`
    - `DashboardSnapshot`
    - `HistoryRecord`
    - `CollectionResponse`

    Create `src/egg_counter/web/server.py` with `create_app(settings: dict, zone_config: dict, pipeline: EggCounterPipeline | None = None) -> FastAPI` and register these exact routes:
    - `GET /` -> dashboard HTML response placeholder for Plan 02 template wiring
    - `GET /history` -> history HTML response placeholder for Plan 02 template wiring
    - `GET /api/dashboard`
    - `GET /api/history`
    - `POST /api/collect`
    - `GET /health`
    - `GET /ws-meta`
    - `websocket /ws`

    Implement exact query parameters:
    - `/api/dashboard?period=weekly|monthly|yearly`
    - `/api/history?size=<value>&from=<YYYY-MM-DD>&to=<YYYY-MM-DD>&limit=<int>`

    Implement `/api/collect` so it:
    - Computes the current count from the dashboard snapshot for today
    - Calls `EggDatabaseLogger(settings["db_path"]).log_eggs_collected(count)`
    - Returns `{message, collected_count, snapshot}`
    - Broadcasts an `eggs_collected` event built from the updated snapshot

    Implement `/ws` to accept the connection, send an initial `snapshot` message immediately, and keep the socket alive until disconnect.

    Create `tests/test_websocket.py` with exact tests:
    - `test_websocket_receives_initial_snapshot`
    - `test_websocket_receives_egg_detected_event`
    - `test_websocket_disconnect_cleans_up_connection`

    Extend `tests/test_web_api.py` with exact FastAPI route tests:
    - `test_dashboard_api_returns_snapshot`
    - `test_history_api_applies_filters`
    - `test_collect_api_persists_collection_and_returns_updated_snapshot`
    - `test_health_route_returns_ok`
  </action>
  <verify>
    <automated>cd D:/codeNcraft/Egg-counter && python -m pytest tests/test_web_api.py tests/test_websocket.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/egg_counter/web/realtime.py contains `class WebSocketHub`
    - src/egg_counter/web/realtime.py contains `async def broadcast_json`
    - src/egg_counter/web/schemas.py contains `class DashboardSnapshot`
    - src/egg_counter/web/server.py contains `def create_app(`
    - src/egg_counter/web/server.py contains `@app.get("/api/dashboard")`
    - src/egg_counter/web/server.py contains `@app.get("/api/history")`
    - src/egg_counter/web/server.py contains `@app.post("/api/collect")`
    - src/egg_counter/web/server.py contains `@app.websocket("/ws")`
    - src/egg_counter/web/server.py contains `@app.get("/health")`
    - tests/test_web_api.py contains `test_collect_api_persists_collection_and_returns_updated_snapshot`
    - tests/test_websocket.py contains `test_websocket_receives_initial_snapshot`
    - tests/test_websocket.py contains `test_websocket_receives_egg_detected_event`
    - `python -m pytest tests/test_web_api.py tests/test_websocket.py -x -q` exits 0
  </acceptance_criteria>
  <done>FastAPI app, WebSocket hub, JSON contracts, and collection/history/dashboard endpoints exist and are covered by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the runtime so pipeline events reach WebSocket clients, resolve collection semantics, and make the server launchable from the CLI</name>
  <files>src/egg_counter/pipeline.py, src/egg_counter/tracker.py, src/egg_counter/cli.py, src/egg_counter/web/server.py, tests/test_web_api.py, tests/test_websocket.py, tests/test_pipeline.py</files>
  <read_first>
    - src/egg_counter/pipeline.py
    - src/egg_counter/tracker.py
    - src/egg_counter/cli.py
    - src/egg_counter/web/server.py
    - tests/test_pipeline.py
  </read_first>
  <action>
    Update `EggCounterPipeline.__init__` to accept an optional `event_callback: Callable[[dict], None] | None = None` and store it as `self.event_callback`.

    Update `process_frame()` so every `log_entry` appended to `logged_events` also triggers:
    ```python
    if self.event_callback is not None:
        self.event_callback(log_entry)
    ```

    Extend `src/egg_counter/web/server.py` with an event bridge function that:
    - Receives persisted pipeline events
    - Rebuilds the latest dashboard snapshot through `EggRepository`
    - Broadcasts `egg_detected` or `eggs_collected` via `WebSocketHub.build_snapshot_event()`
    - Uses toast text exactly `1 new egg added` for single new-egg events

    Resolve the collection-semantic conflict between the tracker and the dashboard-confirmed `Collected` action. Implement one exact Phase 3 rule: when `settings["collection_mode"] == "manual"`, the runtime must not auto-persist `{"action": "collected"}` events generated by `EggTracker`; only the confirmed `POST /api/collect` flow may write collection events in serve mode.

    Make that rule concrete with code changes:
    - add a `collection_mode` setting lookup in `EggCounterPipeline`
    - guard the `elif event["action"] == "collected":` branch in `process_frame()` so manual mode skips `log_eggs_collected(...)`
    - leave `EggTracker` intact for standalone detection unless a tiny hook is required for tests
    - add a regression test proving tracker-generated collection events do not hit SQLite in manual mode

    Add a new CLI subcommand in `src/egg_counter/cli.py` named exactly `serve` with flags:
    - `--model`
    - `--camera`
    - `--config`
    - `--zone`
    - `--video`
    - `--host`
    - `--port`

    Implement the `serve` command so it:
    - Loads settings and zone config
    - Builds the FastAPI app with `create_app(...)`
    - Creates an `EggCounterPipeline(..., event_callback=...)`
    - Starts Uvicorn on the configured host/port
    - Keeps the route and pipeline wiring in the same runtime path so live updates remain in-process

    Add tests or assertions in `tests/test_web_api.py` for `create_app()`/route registration and in `tests/test_websocket.py` for callback-driven broadcast behavior. If CLI tests are added, use exact function name `test_cli_serve_help`.
  </action>
  <verify>
    <automated>cd D:/codeNcraft/Egg-counter && python -m pytest tests/test_web_api.py tests/test_websocket.py tests/test_pipeline.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/egg_counter/pipeline.py contains `event_callback`
    - src/egg_counter/pipeline.py contains `self.event_callback(log_entry)`
    - src/egg_counter/pipeline.py contains `collection_mode`
    - src/egg_counter/pipeline.py contains `if event["action"] == "collected"`
    - src/egg_counter/cli.py contains `serve`
    - src/egg_counter/cli.py contains `args.command == "serve"`
    - src/egg_counter/cli.py contains `--host`
    - src/egg_counter/cli.py contains `--port`
    - src/egg_counter/web/server.py contains `1 new egg added`
    - src/egg_counter/web/server.py contains `uvicorn`
    - tests/test_pipeline.py contains `manual` or `collection_mode`
    - `python -m pytest tests/test_web_api.py tests/test_websocket.py tests/test_pipeline.py -x -q` exits 0
  </acceptance_criteria>
  <done>The detection pipeline can publish persisted events into the web runtime, and the project has a single CLI entry point to serve the local-network dashboard with live updates.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_web_api.py tests/test_websocket.py tests/test_pipeline.py -x -q`
2. `python -m egg_counter.cli serve --help`
3. `python -c "from egg_counter.web.server import create_app; print('web app ok')"`
4. `python -c "from egg_counter.web.realtime import WebSocketHub; print('hub ok')"`
5. `python -c "from egg_counter.repository import EggRepository; print('repository ok')"`
</verification>

<success_criteria>
- FastAPI server and WebSocket hub exist inside the Python monolith
- Dashboard/history/collection routes read from SQLite through repository/service methods
- Pipeline events are broadcast to browser clients without polling
- Manual dashboard collection is the only persisted collection path in Phase 3 serve mode
- Collection action persists and returns an updated snapshot
- CLI provides a `serve` path for local-network access
- Backend tests cover REST and WebSocket behavior
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-dashboard/03-01-SUMMARY.md`
</output>
