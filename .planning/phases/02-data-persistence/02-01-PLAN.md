---
phase: 02-data-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/egg_counter/db.py
  - src/egg_counter/repository.py
  - src/egg_counter/pipeline.py
  - config/settings.yaml
  - tests/conftest.py
  - tests/test_db.py
  - tests/test_repository.py
  - tests/test_pipeline.py
autonomous: true
requirements:
  - DATA-01

must_haves:
  truths:
    - "Detected eggs are written to a SQLite database immediately upon detection"
    - "After a simulated reboot (new logger instance), today's egg count resumes from the database"
    - "User can query historical egg production data grouped by day and by size category"
    - "Pipeline uses SQLite logger instead of JSONL logger with no change to process_frame behavior"
    - "System fails fast with a clear error if the database path is not writable"
  artifacts:
    - path: "src/egg_counter/db.py"
      provides: "EggDatabaseLogger class -- drop-in replacement for EggEventLogger"
      exports: ["EggDatabaseLogger"]
      contains: "class EggDatabaseLogger"
    - path: "src/egg_counter/repository.py"
      provides: "EggRepository class -- read-only query interface for Phase 3"
      exports: ["EggRepository"]
      contains: "class EggRepository"
    - path: "tests/test_db.py"
      provides: "Unit tests for EggDatabaseLogger"
      contains: "test_log_egg_detected_persists"
    - path: "tests/test_repository.py"
      provides: "Unit tests for EggRepository"
      contains: "test_get_daily_summary"
    - path: "config/settings.yaml"
      provides: "db_path configuration key"
      contains: "db_path"
  key_links:
    - from: "src/egg_counter/pipeline.py"
      to: "src/egg_counter/db.py"
      via: "import swap: from egg_counter.db import EggDatabaseLogger"
      pattern: "from egg_counter\\.db import EggDatabaseLogger"
    - from: "src/egg_counter/db.py"
      to: "SQLite database file"
      via: "sqlite3.connect with WAL mode and synchronous=FULL"
      pattern: "PRAGMA journal_mode=WAL"
    - from: "src/egg_counter/repository.py"
      to: "SQLite database file"
      via: "sqlite3.connect with Row factory for dict-like access"
      pattern: "row_factory = sqlite3.Row"
---

<objective>
Replace the JSONL-based EggEventLogger with a SQLite-backed EggDatabaseLogger and add a query/DAO class (EggRepository) for Phase 3 consumption.

Purpose: Make egg detection events durable (survive power loss/reboot), queryable by date range and size category, and provide the data layer Phase 3's dashboard needs.
Output: Working SQLite persistence layer with drop-in logger replacement, query interface, full test coverage, and pipeline integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-persistence/02-CONTEXT.md
@.planning/phases/02-data-persistence/02-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/egg_counter/logger.py:
```python
class EggEventLogger:
    def __init__(self, log_dir: str = "logs") -> None:
        self.egg_count = 0

    def log_egg_detected(
        self,
        track_id: int,
        size: str,
        confidence: float,
        bbox: list,
        size_method: str,
        raw_measurement_mm: float,
        frame_number: int,
    ) -> dict:
        # Returns event dict with keys: type, timestamp, track_id, size, confidence, bbox, size_method, raw_measurement_mm, frame_number
        ...

    def log_eggs_collected(self, count: int) -> dict:
        # Returns event dict with keys: type, timestamp, count
        # Resets self.egg_count to 0
        ...
```

From src/egg_counter/pipeline.py (line 44):
```python
self.logger = EggEventLogger(settings.get("log_dir", "logs"))
```

From src/egg_counter/config.py:
```python
def load_settings(path: str = "config/settings.yaml") -> dict:
def load_zone_config(path: str = "config/zone.json") -> dict:
```

From config/settings.yaml (current content):
```yaml
camera_index: 0
confidence_threshold: 0.5
stability_seconds: 3
log_dir: "logs"
frame_rate: 3
bytetrack_config: "config/bytetrack_eggs.yaml"
location:
  lat: 40.0
  lon: -75.0
```

From tests/conftest.py (existing fixtures):
```python
@pytest.fixture
def tmp_log_dir(tmp_path): ...

@pytest.fixture
def tmp_settings_file(tmp_path): ...

@pytest.fixture
def tmp_zone_file(tmp_path): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create EggDatabaseLogger with SQLite persistence and tests</name>
  <files>src/egg_counter/db.py, tests/test_db.py, tests/conftest.py, config/settings.yaml</files>
  <read_first>
    - src/egg_counter/logger.py (interface to replicate exactly)
    - src/egg_counter/pipeline.py (lines 44, 126-141 -- how logger is called)
    - config/settings.yaml (add db_path key)
    - tests/conftest.py (add tmp_db_path fixture, follow existing patterns)
    - tests/test_logger.py (test style reference)
  </read_first>
  <behavior>
    - test_log_egg_detected_persists: Create logger with tmp_path db, call log_egg_detected(track_id=1, size="large", confidence=0.95, bbox=[100.0,100.0,200.0,200.0], size_method="bbox_ratio", raw_measurement_mm=58.3, frame_number=100), close logger, open raw sqlite3 connection to same path, SELECT * FROM egg_events -> 1 row with matching values
    - test_log_egg_detected_returns_event_dict: Call log_egg_detected, returned dict has keys type="egg_detected", timestamp (ISO 8601), track_id=1, size="large", confidence=0.95, bbox=[100.0,100.0,200.0,200.0], size_method="bbox_ratio", raw_measurement_mm=58.3, frame_number=100
    - test_count_restores_on_restart: Create logger, log 3 eggs, close it. Create new EggDatabaseLogger with same db_path -> new_logger.egg_count == 3
    - test_log_eggs_collected: Create logger, log 2 eggs (egg_count==2), call log_eggs_collected(count=2) -> egg_count==0, returned dict has type="eggs_collected" and count=2, raw sqlite3 query on collection_events table -> 1 row with count=2
    - test_fail_fast_unwritable: On Linux/Mac, create a read-only directory, attempt EggDatabaseLogger(path_in_readonly_dir) -> raises PermissionError. (Skip on Windows if os.access unreliable.)
    - test_wal_mode_enabled: Create logger, query PRAGMA journal_mode -> "wal"
    - test_schema_version_set: Create logger, query PRAGMA user_version -> 1
    - test_stdout_output: Call log_egg_detected, capture stdout -> contains "New egg #1" and "large"
  </behavior>
  <action>
    **1. Create `src/egg_counter/db.py`** with class `EggDatabaseLogger`:

    Constructor `__init__(self, db_path: str = "data/eggs.db") -> None`:
    - `db_file = Path(db_path)` then `db_file.parent.mkdir(parents=True, exist_ok=True)`
    - Fail-fast check (per D-09): if file exists and not writable, or parent not writable for new file, raise `PermissionError(f"Database not writable: {db_path}")`
    - `self.conn = sqlite3.connect(db_path)`
    - Call `self._configure_pragmas()` and `self._create_tables()`
    - `self.egg_count = self._get_today_count()` (per D-03)

    `_configure_pragmas(self)`:
    - Execute: `PRAGMA journal_mode=WAL`, `PRAGMA synchronous=FULL`, `PRAGMA foreign_keys=ON`

    `_create_tables(self)`:
    - `conn.executescript(...)` with two CREATE TABLE IF NOT EXISTS statements:
      - `egg_events` table: id INTEGER PRIMARY KEY AUTOINCREMENT, timestamp TEXT NOT NULL, detected_date TEXT NOT NULL, track_id INTEGER NOT NULL, size TEXT NOT NULL, confidence REAL NOT NULL, bbox_x1 REAL NOT NULL, bbox_y1 REAL NOT NULL, bbox_x2 REAL NOT NULL, bbox_y2 REAL NOT NULL, size_method TEXT NOT NULL, raw_measurement_mm REAL NOT NULL, frame_number INTEGER NOT NULL
      - `collection_events` table: id INTEGER PRIMARY KEY AUTOINCREMENT, timestamp TEXT NOT NULL, collected_date TEXT NOT NULL, count INTEGER NOT NULL
    - Create indexes: `idx_egg_events_date ON egg_events(detected_date)`, `idx_egg_events_size ON egg_events(detected_date, size)`, `idx_collection_date ON collection_events(collected_date)`
    - Set `PRAGMA user_version = 1`

    `_get_today_count(self) -> int`:
    - `today = datetime.now(timezone.utc).strftime("%Y-%m-%d")`
    - `SELECT COUNT(*) FROM egg_events WHERE detected_date = ?` with (today,)
    - Return row[0]

    `log_egg_detected(self, track_id, size, confidence, bbox, size_method, raw_measurement_mm, frame_number) -> dict`:
    - Same signature as EggEventLogger (per D-02)
    - `self.egg_count += 1`
    - `ts = datetime.now(timezone.utc).isoformat()`
    - `detected_date = datetime.now(timezone.utc).strftime("%Y-%m-%d")`
    - Insert into egg_events using `with self.conn:` context manager (auto-commit)
    - Build and return event dict matching EggEventLogger format: `{"type": "egg_detected", "timestamp": ts, "track_id": track_id, "size": size, "confidence": round(confidence, 3), "bbox": bbox, "size_method": size_method, "raw_measurement_mm": round(raw_measurement_mm, 1), "frame_number": frame_number}`
    - Print `f"New egg #{self.egg_count} -- {size}"` (matches existing logger output)

    `log_eggs_collected(self, count: int) -> dict`:
    - Same signature as EggEventLogger (per D-02)
    - `ts = datetime.now(timezone.utc).isoformat()`
    - `collected_date = datetime.now(timezone.utc).strftime("%Y-%m-%d")`
    - Insert into collection_events using `with self.conn:`
    - `self.egg_count = 0`
    - Print `f"Eggs collected: {count}"`
    - Return `{"type": "eggs_collected", "timestamp": ts, "count": count}`

    `close(self) -> None`:
    - `self.conn.close()`

    Import: `import os, sqlite3` from stdlib, `from datetime import datetime, timezone`, `from pathlib import Path`

    **2. Update `config/settings.yaml`** -- add `db_path: "data/eggs.db"` after the `log_dir` line.

    **3. Update `tests/conftest.py`** -- add fixture:
    ```python
    @pytest.fixture
    def tmp_db_path(tmp_path):
        """Provide a temporary SQLite database path."""
        return str(tmp_path / "test_eggs.db")
    ```

    **4. Create `tests/test_db.py`** with class `TestEggDatabaseLogger` containing all behaviors listed above. Use `tmp_db_path` fixture. For the fail-fast test, use `pytest.mark.skipif(sys.platform == "win32", reason="os.access unreliable on Windows")`.
  </action>
  <verify>
    <automated>cd D:/codeNcraft/Egg-counter && python -m pytest tests/test_db.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/egg_counter/db.py contains `class EggDatabaseLogger`
    - src/egg_counter/db.py contains `def log_egg_detected(self, track_id: int, size: str, confidence: float, bbox: list, size_method: str, raw_measurement_mm: float, frame_number: int) -> dict:`
    - src/egg_counter/db.py contains `def log_eggs_collected(self, count: int) -> dict:`
    - src/egg_counter/db.py contains `PRAGMA journal_mode=WAL`
    - src/egg_counter/db.py contains `PRAGMA synchronous=FULL`
    - src/egg_counter/db.py contains `PRAGMA user_version = 1`
    - src/egg_counter/db.py contains `def close(self)`
    - src/egg_counter/db.py contains `raise PermissionError`
    - config/settings.yaml contains `db_path: "data/eggs.db"`
    - tests/conftest.py contains `def tmp_db_path(tmp_path):`
    - tests/test_db.py contains `class TestEggDatabaseLogger`
    - tests/test_db.py contains `test_log_egg_detected_persists`
    - tests/test_db.py contains `test_count_restores_on_restart`
    - tests/test_db.py contains `test_fail_fast_unwritable`
    - `python -m pytest tests/test_db.py -x` exits 0
  </acceptance_criteria>
  <done>EggDatabaseLogger class created with identical interface to EggEventLogger, SQLite WAL persistence, startup count restoration, fail-fast on unwritable path, and all unit tests passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create EggRepository query/DAO class with tests</name>
  <files>src/egg_counter/repository.py, tests/test_repository.py</files>
  <read_first>
    - src/egg_counter/db.py (schema definition and table structure created in Task 1)
    - .planning/phases/02-data-persistence/02-RESEARCH.md (query patterns, DAO interface from Pattern 2)
  </read_first>
  <behavior>
    - test_get_daily_summary: Insert 3 eggs (2 large, 1 medium) for 2026-03-20 into egg_events table directly via sqlite3, call get_daily_summary(date(2026, 3, 20)) -> returns {"date": "2026-03-20", "total": 3, "by_size": {"large": 2, "medium": 1}}
    - test_get_daily_summary_empty_date: Call get_daily_summary for a date with no data -> returns {"date": "2026-03-20", "total": 0, "by_size": {}}
    - test_get_eggs_by_date_range: Insert eggs on 3 different dates (2026-03-18: 2 eggs, 2026-03-19: 3 eggs, 2026-03-20: 1 egg), call get_eggs_by_date_range(date(2026,3,18), date(2026,3,20)) -> returns [{"date": "2026-03-18", "total": 2}, {"date": "2026-03-19", "total": 3}, {"date": "2026-03-20", "total": 1}]
    - test_get_eggs_by_date_range_empty: Call with range that has no data -> returns []
    - test_get_size_breakdown: Insert 5 eggs (2 large, 2 medium, 1 jumbo) across 2 days in range, call get_size_breakdown(start, end) -> returns {"large": 2, "medium": 2, "jumbo": 1}
    - test_get_size_breakdown_empty: Call with empty range -> returns {}
  </behavior>
  <action>
    **1. Create `src/egg_counter/repository.py`** with class `EggRepository`:

    Constructor `__init__(self, db_path: str = "data/eggs.db") -> None`:
    - `self.conn = sqlite3.connect(db_path)`
    - `self.conn.row_factory = sqlite3.Row`

    `get_daily_summary(self, target_date: date) -> dict`:
    - Query: `SELECT size, COUNT(*) as count FROM egg_events WHERE detected_date = ? GROUP BY size` with `(target_date.isoformat(),)`
    - Build `by_size` dict from rows: `{row["size"]: row["count"] for row in rows}`
    - Return `{"date": target_date.isoformat(), "total": sum(by_size.values()), "by_size": by_size}`

    `get_eggs_by_date_range(self, start: date, end: date) -> list[dict]`:
    - Query: `SELECT detected_date, COUNT(*) as total FROM egg_events WHERE detected_date BETWEEN ? AND ? GROUP BY detected_date ORDER BY detected_date` with `(start.isoformat(), end.isoformat())`
    - Return `[{"date": row["detected_date"], "total": row["total"]} for row in rows]`

    `get_size_breakdown(self, start: date, end: date) -> dict`:
    - Query: `SELECT size, COUNT(*) as count FROM egg_events WHERE detected_date BETWEEN ? AND ? GROUP BY size` with `(start.isoformat(), end.isoformat())`
    - Return `{row["size"]: row["count"] for row in rows}`

    `close(self) -> None`:
    - `self.conn.close()`

    Import: `import sqlite3`, `from datetime import date`

    **2. Create `tests/test_repository.py`** with class `TestEggRepository`.

    Use a helper function `_insert_egg(conn, detected_date, size)` that inserts a row into egg_events with dummy values for non-essential columns (timestamp=detected_date+"T12:00:00+00:00", track_id=1, confidence=0.9, bbox_x1/y1/x2/y2=0.0, size_method="bbox_ratio", raw_measurement_mm=50.0, frame_number=1).

    Each test creates a `:memory:` database, runs the schema creation SQL from db.py (copy the CREATE TABLE and CREATE INDEX statements), inserts test data, then creates EggRepository pointing at the same connection. To share the connection, use a fixture that creates an in-memory db, runs schema SQL, and returns the path. Actually, since `:memory:` databases cannot be shared between connections, use `tmp_path` fixture instead: create db file with schema, insert data, then open EggRepository with same path.

    Alternative simpler approach: Create EggDatabaseLogger with tmp_db_path to initialize schema, close it, then insert test data and create EggRepository with same path. This avoids duplicating schema SQL.
  </action>
  <verify>
    <automated>cd D:/codeNcraft/Egg-counter && python -m pytest tests/test_repository.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/egg_counter/repository.py contains `class EggRepository`
    - src/egg_counter/repository.py contains `def get_daily_summary(self, target_date: date) -> dict:`
    - src/egg_counter/repository.py contains `def get_eggs_by_date_range(self, start: date, end: date) -> list[dict]:`
    - src/egg_counter/repository.py contains `def get_size_breakdown(self, start: date, end: date) -> dict:`
    - src/egg_counter/repository.py contains `row_factory = sqlite3.Row`
    - src/egg_counter/repository.py contains `def close(self)`
    - tests/test_repository.py contains `class TestEggRepository`
    - tests/test_repository.py contains `test_get_daily_summary`
    - tests/test_repository.py contains `test_get_eggs_by_date_range`
    - tests/test_repository.py contains `test_get_size_breakdown`
    - `python -m pytest tests/test_repository.py -x` exits 0
  </acceptance_criteria>
  <done>EggRepository class created with get_daily_summary, get_eggs_by_date_range, and get_size_breakdown methods, all returning structured dicts suitable for Phase 3 charting, with full test coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Wire pipeline to use EggDatabaseLogger and update integration tests</name>
  <files>src/egg_counter/pipeline.py, tests/test_pipeline.py</files>
  <read_first>
    - src/egg_counter/pipeline.py (current logger import and instantiation on line 16 and 44)
    - src/egg_counter/db.py (the new EggDatabaseLogger class created in Task 1)
    - tests/test_pipeline.py (existing integration tests -- need to update _make_settings and mock paths)
  </read_first>
  <action>
    **1. Update `src/egg_counter/pipeline.py`**:

    Change import on line 16 from:
    ```python
    from egg_counter.logger import EggEventLogger
    ```
    to:
    ```python
    from egg_counter.db import EggDatabaseLogger
    ```

    Change line 44 from:
    ```python
    self.logger = EggEventLogger(settings.get("log_dir", "logs"))
    ```
    to:
    ```python
    self.logger = EggDatabaseLogger(settings.get("db_path", "data/eggs.db"))
    ```

    Add cleanup in the `run()` method's `finally` block (after `cap.release()`, before the print):
    ```python
    if hasattr(self.logger, 'close'):
        self.logger.close()
    ```

    No other changes to pipeline.py. The process_frame method calls `self.logger.log_egg_detected(...)` and `self.logger.log_eggs_collected(...)` which have identical signatures (per D-02).

    **2. Update `tests/test_pipeline.py`**:

    Update `_make_settings()` to include `db_path` key instead of (or in addition to) `log_dir`:
    ```python
    def _make_settings():
        return {
            "stability_seconds": 3,
            "db_path": "data/eggs.db",
            "log_dir": "logs",
            "frame_rate": 3,
            "confidence_threshold": 0.5,
            "bytetrack_config": "config/bytetrack_eggs.yaml",
        }
    ```

    Update the `@patch` decorators: every test that patches `"egg_counter.pipeline.EggDetector"` also needs to patch the logger. Add `@patch("egg_counter.pipeline.EggDatabaseLogger")` to each test function (or use a class-level patch). The mock logger should have `log_egg_detected` and `log_eggs_collected` return event dicts.

    Specifically, update each test:
    - `test_process_frame_new_egg`: Add `@patch("egg_counter.pipeline.EggDatabaseLogger")` decorator. The mock auto-creates the logger, so `pipeline.logger` will be a MagicMock already. Ensure `pipeline.logger.log_egg_detected.return_value` returns a dict with `{"type": "egg_detected", "track_id": 1, "size": "large", ...}`.
    - `test_process_frame_out_of_zone`: Same decorator addition.
    - `test_pipeline_restart_initialization`: Same decorator addition.
    - `test_process_frame_stability_timing`: Same decorator addition.

    The key insight: since EggCounterPipeline.__init__ calls `EggDatabaseLogger(settings.get("db_path", ...))`, patching the class at module level prevents actual SQLite connections in tests.
  </action>
  <verify>
    <automated>cd D:/codeNcraft/Egg-counter && python -m pytest tests/test_pipeline.py tests/test_db.py tests/test_repository.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/egg_counter/pipeline.py contains `from egg_counter.db import EggDatabaseLogger`
    - src/egg_counter/pipeline.py does NOT contain `from egg_counter.logger import EggEventLogger`
    - src/egg_counter/pipeline.py contains `EggDatabaseLogger(settings.get("db_path", "data/eggs.db"))`
    - src/egg_counter/pipeline.py contains `self.logger.close()`
    - tests/test_pipeline.py contains `EggDatabaseLogger`
    - tests/test_pipeline.py contains `db_path`
    - `python -m pytest tests/test_pipeline.py -x` exits 0
    - `python -m pytest tests/ -x` exits 0 (full suite passes)
  </acceptance_criteria>
  <done>Pipeline uses EggDatabaseLogger instead of EggEventLogger. Logger connection is closed on pipeline shutdown. All existing pipeline tests updated and passing. Full test suite green.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -x -v` -- full test suite passes (all existing + new tests)
2. `python -c "from egg_counter.db import EggDatabaseLogger; print('import ok')"` -- import succeeds
3. `python -c "from egg_counter.repository import EggRepository; print('import ok')"` -- import succeeds
4. `python -c "from egg_counter.pipeline import EggCounterPipeline; print('import ok')"` -- import succeeds with new logger
5. Grep verification: `grep -r "EggEventLogger" src/egg_counter/pipeline.py` returns nothing (old logger fully removed from pipeline)
</verification>

<success_criteria>
- EggDatabaseLogger is a drop-in replacement for EggEventLogger with identical method signatures (per D-02)
- SQLite database created with WAL mode and synchronous=FULL for power-loss durability
- Startup count restoration works via SELECT COUNT query (per D-03)
- EggRepository provides get_daily_summary, get_eggs_by_date_range, get_size_breakdown for Phase 3
- Pipeline.py imports and uses EggDatabaseLogger (per D-01)
- db_path configurable via settings.yaml (per D-07)
- Fail-fast on unwritable path (per D-09)
- Full test suite passes including new and updated tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-persistence/02-01-SUMMARY.md`
</output>
